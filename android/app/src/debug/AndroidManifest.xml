<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Flutter debugging only; the release manifest has no network permission. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
